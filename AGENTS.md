# Repository Instructions

## Versioning

- For every code change, bump the minor version in `Cargo.toml`.
- Keep `Cargo.lock` in sync with the `Cargo.toml` package version.
- Do not hardcode the CLI version separately from Cargo package metadata.
